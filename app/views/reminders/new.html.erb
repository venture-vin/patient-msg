
<%= form_for :reminder do |f| %>
  <p>
    <%= f.label :title %><br>
    <%= f.collection_select :provider_id, Patient.order(:last_name),:id,:name, include_blank: true %>
  </p>

  <p>
    <%= f.label :text %><br>
    <%= f.text_area :text %>
  </p>

  <p>
    <%= f.submit %>
  </p>
<% end %>
